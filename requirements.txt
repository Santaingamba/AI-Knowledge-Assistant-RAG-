# This file is intentionally a pointer.
# The authoritative requirements file is backend/requirements.txt,
# which is what the Dockerfile installs from.
#
# To install backend dependencies locally:
#   pip install -r backend/requirements.txt
#
# Frontend dependencies are managed via frontend/package.json.
